<div class="mann-header mann-header--hearth">
	<%= render partial: 'pages/collections/hearth/menu' %>
	<%# stick the hero image on the home page only %>
	<% if current_page?('/collections/hearth') %>
		<div class="hero">
			<div class="container">
				<div class="search-bar search-bar-home row">
					<div class="col-md-8">
						<%= render '/catalog/search_form', { :@collection_search => "Home Economics Archive: Research, Tradition and History"}  %>
					</div>
				</div>
			</div>
		</div>
	<% else %>
		<div class="search-bar">
			<div class="container">
				<div class="col-md-8">
					<%= render '/catalog/search_form', { :@collection_search => "Home Economics Archive: Research, Tradition and History"}  %>
				</div>
			</div>
		</div>
	<% end %>	
</div>