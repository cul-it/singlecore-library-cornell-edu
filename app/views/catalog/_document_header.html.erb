<%# bookmark functions for items/docs -%>
<%= render_index_doc_actions document, :wrapping_class => "select-item" %>
 <h2 class="blacklight-title_display">

	 <%= t('blacklight.search.documents.counter', :counter => (document_counter + 1 + @response.params[:start].to_i)) %>

	<%= link_to_document document, :label=>document_show_link_field(document), :counter => (document_counter + 1 + @response.params[:start].to_i), :subject => id_to_subject(render_index_field_value :document => document, :field => 'collection_tesim') %> 
</h2>