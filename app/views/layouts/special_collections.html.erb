<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">

    <!-- Mobile viewport optimization h5bp.com/ad -->
    <meta name="HandheldFriendly" content="True">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    
    <!-- Internet Explorer use the highest version available -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <!-- Mobile IE allows us to activate ClearType technology for smoothing fonts for easy reading -->
    <!--[if IEMobile]>
      <meta http-equiv="cleartype" content="on">
    <![endif]-->

    <title><%= render_page_title %></title>
    <%= opensearch_description_tag application_name, opensearch_catalog_url(:format => 'xml') %>
    <%= favicon_link_tag 'favicon.png' %>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= content_for(:head) %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  </head>

  <body class="<%= render_body_class %>">

    <%= render :partial => 'shared/cornell' %>
    <%= render :partial => 'shared/header_navbar' %>

    <%# this comes from blacklight, you might not need it on pages? %>
    <%= render partial: 'shared/ajax_modal' %>
    <div class="special-collections">

        <%# TODO: change this to if url contains /collections/chla so it loads on all chla pages %>
        <% if current_page?('/collections/chla') %>
            <%= render partial: 'pages/collections/chla/header' %>
        <% end %>

        <%# Tarr home page %>
        <% if current_page?('/collections/tarr') %>
            <%= render partial: 'pages/collections/tarr/home' %>
        <% end %>

        <%# Sri Lanka home page %>
        <% if current_page?('/collections/srilanka') %>
            <%= render partial: 'pages/collections/srilanka/header' %>
        <% end %>

        <div class="container">
            <%= yield %>
        </div>
    </div>
    
    <%= render :partial => 'shared/footer' %>
  </body>
</html>
