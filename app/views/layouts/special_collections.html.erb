<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>

    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-MPDTMRL');</script>
    <!-- End Google Tag Manager -->
    
    <meta charset="utf-8">
    <meta http-equiv="Content-Type" content="text/html">

    <!-- Mobile viewport optimization h5bp.com/ad -->
    <meta name="HandheldFriendly" content="True">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">

    <% if ENV["COLLECTIONS"] == "development" %>
      <meta name="robots" content="noindex,nofollow">
      <meta name="google-site-verification" content="cwcy4xOe9mX8qQ-GINjaytbltDcGsq8HraFxA_SN89E">
    <% end %>
    
    <!-- Internet Explorer use the highest version available -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <!-- Mobile IE allows us to activate ClearType technology for smoothing fonts for easy reading -->
    <!--[if IEMobile]>
      <meta http-equiv="cleartype" content="on">
    <![endif]-->

    <title><%= yield(:page_title) %> | <%= application_name %></title>
    <%= opensearch_description_tag application_name, opensearch_catalog_url(:format => 'xml') %>
    <%= favicon_link_tag 'favicon.png' %>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= content_for(:head) %>

    <%= analytics_init if GoogleAnalytics.valid_tracker? %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  </head>

  <body class="<%= render_body_class %>">
    <% if ENV["COLLECTIONS"] == "development" %>
        <%= render :partial => 'shared/stg_message' %>
    <% end %>

    <% if ENV["SERVICE_ALERT"] == "on" %>
        <%= render partial: 'shared/alert' %>
    <% end %>

    <%= render :partial => 'shared/cornell' %>
    <%= render :partial => 'shared/header_navbar_collections' %>

    <%# this comes from blacklight, you might not need it on pages? %>
    <%= render partial: 'shared/ajax_modal' %>
    <div class="special-collections" role="main" id="maincontent">
        <%= yield %>
    </div>
    
    <%= render :partial => 'shared/footer' %>
  </body>
</html>
